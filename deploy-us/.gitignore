# 本地实际配置（含真实密码），不入库；只提交 .env.example 作模板
.env
.env.local
# 兜底：万一 DATA_DIR 被改回仓库内相对路径，也不让数据/备份进库
components/
# build-and-save.sh 的产出物（镜像 tar + 从源码打包的 config），不入库
openim-config/
chat-config/
openim-images-*.tar.gz
